#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(int argc, char** argv)
{
	/* long double pi = 1; */
	/* int terms = atoi(argv[1]); */
	/* for (int i = 1; i < terms; ++i) */
	/* 	pi += 4 * (pow(-1.0, i))/((2 * i) + 1); */
	/* pi += 3; */
	printf("%f\n", M_PI);
	return 0;
}
